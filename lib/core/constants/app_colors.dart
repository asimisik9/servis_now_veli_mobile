import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF073662); // Koyu Lacivert
  static const Color accent = Color(0xFF18A1D8); // Açık Mavi
  static const Color base = Color(0xFFFFFFFF); // Beyaz
  static const Color textPrimary = Color(0xFF073662);
  static const Color textSecondary = Colors.grey;
  static const Color error = Colors.red;
}
